#include "main.h"

/**
  * _isupper - that checks for uppercase character.
  * @c: character
  *
  * Return: 1 o 0
  */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
