#include "main.h"

/**
  * _isdigit - Write a function that checks for
  * @c: character
  *
  * Return: 1 o 0
  */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
